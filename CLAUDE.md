# CLAUDE.md — 旅行フォトビンゴ

このファイルは Claude Code が作業する際のガイド。**作業前に必ず全部読むこと。**

## これは何

8人グループの旅行イベント用「**ドパガキ卒業フォトビンゴ**」Webアプリ。**本番運用中**（友人が実際に使用中）。
コンセプトは「ドパガキ治療プログラム」＝スマホ（スクリーン）の時間を減らして、現実世界にあるお題をじっくり探すビンゴ。文言はこのトーン（ポップさは維持）で書く。

- 公開URL: https://1046nee.github.io/photo-bingo/
- リポジトリ: https://github.com/1046nee/photo-bingo （mainブランチ）
- アプリ本体は `index.html` 1ファイルのみ（ビルドなし・フレームワークなし・素のES Modules）
- 反映手順は「編集 → commit → push(main)」だけ。GitHub Pagesが1〜2分で自動反映する

## ⚠️ 最重要：既存データ保護ルール

本番運用中のため、以下を破ると本番の写真・データが壊れる／参照できなくなる。
**2026-08-04以降、メンバーが実際に写真をアップし始めている（実データ蓄積中）。どんな更新でも写真・ボードデータを消さない・ズラさないことを最優先に確認してからpushすること（変更diffに store層・GRID・MEMBERS・保存キーが含まれていないかを毎回チェック）。**

1. **マスIDは位置由来**（`r{行}c{列}`、例 r0c1）。`GRID` の並び替え・お題の位置変更は**禁止**。写真は `photos/{名前}_{マスID}` に紐づいているため、位置を変えると全員の写真がズレる
2. **`MEMBERS` の名前はFirestoreのドキュメントキー**。表記を1文字でも変えるとその人の `boards/{旧名}` と `photos/{旧名}_*` が孤立する。変更するならFirestore側のデータ移行とセット
3. **FREEは四隅前提**。動かすなら `FREE_IDS`・`calcScore`（+4の定数）・`GRID` をすべて整合させること
4. **写真base64は1ドキュメント1MB制限**（Firestoreの仕様＋ルールで `img.size() < 1000000` を強制）。`compress` の上限（90万文字目標・98万でreject）を緩めるならルールも要変更
5. **無料枠**：読み取り5万/日、egress 10GiB/月。個人ボード閲覧は最大60読み取り/回。全員の写真を一括表示する改修は読み取り数に注意
6. **修正は必ずお試しモード（後述）で動作確認してからpush**。本番Firestoreに向けたままテストしない

## 技術構成

- フロント: `index.html` 単体。フォント Zen Maru Gothic（Google Fonts）、配色はCSS変数（`:root`、テーマ色 `--purple:#3C3489`）。ヘッダーは紫系グラデーションバナー（`.hd`・抑えめ）＋💊錠剤型バッジ。下の要素には紫→ピンクの差し色を要所だけ（h2.secの縦線・点数の数字・進捗バー・ステージバッジ）＝全体は落ち着き、要素にドパガキ感
- バックエンド: Firebase Cloud Firestore（**Sparkプラン無料枠**・東京）。SDK v10.12.2 を gstatic CDN から dynamic import
- 写真: Firebase Storageは**不使用**（課金回避）。端末でcanvas圧縮したbase64をFirestoreに直接保存
- 設定ブロック（index.htmlの「▼▼▼ 設定」コメント区間）:
  - `MEMBERS` … 8人の名前（ドキュメントキー兼用）
  - `MALE_MEMBERS` … 名前ボタンが青になるメンバー（それ以外は赤）
  - `RESULTS_AT` … 結果発表日時（+09:00表記を維持）。countdown・あそびかたに `fmt(RESULTS_AT)` で日時を表示している（発表日時を変えるときはここだけ直せば表示も全部変わる）
  - `GM_PASS` … ゲームマスター用パスワード（数字11桁）
  - `FIREBASE_CONFIG` … 本番接続情報（projectId: photo-bingo-4d3c1）

## Firestoreデータ構造

- `boards/{名前}`:
  - `filled: {rXcY: true}` … 埋めたマス
  - `pass: "1234"` … 本人が設定した4桁パスワード（GM画面で閲覧できる仕様）
  - `ok: {rXcY:{ts}}` / `ng: {rXcY:{ts}}` … GM判定（〇/×）。**相互排他**（`setJudge` が一方を設定し他方を削除）
  - `log: [{id,label,ts,type,text?}]` … 履歴。type: `ok`/`ng`/`clear`(GM取消)/`redo`(判定リセット)/`del`(削除)/`up`(新規アップ)/`reup`(撮り直し)/`cmt`(本人コメント更新)/`gmcmt`(GMコメント)。`text`=コメント内容（up/reup/cmt/gmcmtで記録）。マイビンゴ・GMの写真チェック画面・お試しボードに「📋 これまでのきろく」（`histHtml`＝開閉式details・全件を新しい順・内部スクロール）、GM画面に「📋 みんなのきろく」（`gmAllHist`＝全員分マージ・名前付き）で表示。日時は曜日付き（`fmtFull`）、行の背景色: 〇判定=緑・×判定=赤・本人コメント更新=薄青紫・GMコメント=薄オレンジ（.h-ok/.h-ng/.h-cmt/.h-gmcmt）。かっこ内のコメント文字は本人=緑（.ct-me）・GM=オレンジ（.ct-gm）。日付が変わる境目に「── 8/5(火) ──」の区切り線（`histListHtml`＋.histdate）。旧エントリ（textなし）はそのまま互換
  - `photoTs: {rXcY: ts}` … 写真の版番号。`setFilled` が書く。キャッシュ無効化とGM誤判定防止に使用
  - `comments: {rXcY: "text"}` … 写真のひとことコメント（最大100文字・`setComment`で書く。空で削除。写真削除時に一緒に消す。boardsの購読に乗るので読み取りコスト増なし。**表示時は必ずesc()**）
  - `gmComments: {rXcY: "text"}` … GMからのコメント（`setGmComment`。判定モーダルで記入、判定と同時保存も可。本人のマスモーダルに🔑表示。**撮り直し・写真削除で消す**＝前の写真宛ての講評を残さない。表示時は必ずesc()）
  - `updatedAt`
- `photos/{名前}_{マスID}`: `{img: base64文字列, ts}`
- `boards/_demo` + `photos/_demo_*`: GM練習用お試しボード（`DEMO_NAME`）。MEMBERS外なのでランキング・名前選択に出ない
- **×却下アーカイブ**: `photos/reject_{名前}_{マスID}_{ts}`（imgルールを満たすのでルール変更不要）＋インデックス `boards/_rejects`＝`{list:[{key,name,cellId,label,ts,ngTs,photoTs,manual,cmt,note}]}`（boards購読に乗る。cmt=×当時の本人コメント・note=GMコメント・ngTs=×判定日時）。GMが×判定した瞬間に`archiveReject`が自動保存（同一photoTsは重複スキップ・ngTs=保存時刻）。×記録（logのng）とアーカイブの対応付けは`rejForNg`＝name+cellId＋ngTs近傍60秒マッチ。本人が撮り直し・削除してもアーカイブは残る仕様（意図的）
  - **みんなの×判定一覧**（`renderRejects`・view="rejects"・誰でも閲覧可）: トップ（名前選択）下部の「❌ みんなの×判定一覧をみる」から。盤面形式（マスに×回数の赤バッジ・0件は薄表示）→マスタップで`rejCellModal`＝そのお題の×回答を**時系列順（古い→新しい）**に写真＋名前＋日時で表示（「まだの人」は出さない）→写真タップで`rejPhotoModal`＝拡大＋💬🔑コメント
  - **×判定タイムライン**: 一覧ページのビンゴ下に「🕒 さいきんの×判定」＝最新5件をXふうのカード（`rejTimelineItem`＝お題バッジ・相対時刻`fmtRel`（たった今/n分前/n時間前/きのう HH:MM/n日前/日付 HH:MM）・📸名前・写真・💬🔑コメント）で上が最新。「もっと見る」→view="rejtl"（`renderRejTimeline`＝全件フルページ）。アーカイブ写真の枠は`data-rejph`属性（同じ写真を複数箇所で表示できる。`loadRejectPhotos`が一括で埋める）
  - **コメントの表示形式**: どの画面でも頭に発言者名＝「💬 {名前（男女色）}： …」「🔑 ゲームマスター： …」（rejPhotoModal/viewSquare/openSquare/gmSquare/myRejUpload/タイムライン共通）
  - **昔の×写真は本人が再アップ**: マイビンゴのビンゴ下に「❌ 過去の×になった回答」（`myRejHtml`・開閉式・時系列）。アーカイブ未登録の×記録には「📤 アップロードしてください」→タップで`myRejUpload`モーダル＝**写真を選ぶだけ**（当時の本人コメント/GMコメントはログから自動復元して表示のみ・自動付与。履歴にコメントが無ければコメントUI自体を出さず写真だけアップ）→保存で_rejectsに載り、みんなの一覧に即反映
  - **GMの×アーカイブ管理**: GMの写真チェック画面に「❌ ×アーカイブの管理」（`gmRejManageHtml`/`wireGmRejManage`・開閉式）。誤タップ重複などのエントリを個別削除できる（`removeReject`＝_rejectsのlistからarrayRemove＋photos/reject_*も削除。confirm禁止方針なので**2度押し確定・3秒で自動解除**のアーム式）
  - **今も×中の昔の×は自動取り込み**: `rejAutoFillable`（現在ng中＋その×より新しい×がない＋×後に写真未差し替え）なら、`backfillRejects`が**今の写真をそのままアーカイブに自動保存**（コメントは現在のcomments/gmComments優先で当時のものが残る）。実行タイミングはマイビンゴ表示時（本人分）と×判定一覧表示時（全員分）。二重登録はrejForNg＋rejBackfillTriedで防止。本人アップが必要なのは「撮り直しで写真が消えた×」だけ（myRejHtmlは写真あり/⏳自動反映中/📤要アップの3状態表示）
- 得点: 埋めマス1点＋FREE4点＋ライン（8行+8列+斜め2=18本）×2点、満点100。**×のマスは未記入扱いで減点**、〇は表示のみ
- ステージ: 点数で上がる（`STAGES`＝0/10/15/20/30/45/60/80/100点の9段階）。点数ボックスにバッジ＋「つぎまであとX点」ミニバー。ステージが上がった瞬間は`maybeStageBurst`のお祝い演出（同一セッションで前より上がったときだけ発火・初回表示や降格では出ない。マイビンゴとお試しボードのみ）
- 完成したライン上のマスは金色に光る（`litCells()`＝calcScoreと同じ判定で完成ラインのマスID集合を返し、`cellHtml`が`.lit`クラスを付与。FREEマスも光る。全ボード画面共通）

## Firestoreルール（デプロイ済み。リポジトリ外なのでここに控え）

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /boards/{doc} { allow read, write: if true; }
    match /photos/{doc} {
      allow read, delete: if true;
      allow create, update: if request.resource.data.img is string
                            && request.resource.data.img.size() < 1000000;
    }
  }
}
```

## 画面フロー

1. 名前選択（2列配置：左列=男子・青、右列=女子・赤。**「男子/女子」の文字は画面に出さない**）→ 5桁パスワード（初回は設定＋「GMが確認できるので使い回し禁止」の注意。何度でも試せる）→ マイビンゴ。**認証は端末に記憶しない**＝開くたび・名前を選ぶたびに毎回パスワード入力（「ログイン/ログアウト」という概念は無し）
2. マイビンゴ: マスをタップ→写真アップ（締切なし・いつでも追加/撮り直し/削除）＋💬コメント（写真選択後に欄が出る。既存写真は「コメントを保存」で文だけ更新可）。**「名前変更」ボタンは廃止＝ヘッダー（.hd）をタップするとどの画面からもホーム（名前選択）に戻る**（me/gmOK等を全リセット。ヘッダーに「🏠 ここをタップでホームへ」の極小ヒントあり）。表の下に点数ボックス・GMチェック履歴・お題一覧（`topicsHtml(board)`＝自分の状態付き: 確認中/OK/要修正、カテゴリ別n/mコ。名前選択画面では引数なし＝状態なし一覧）
3. GM画面: 名前選択画面下の「🔑 ゲームマスターはこちら」→ `GM_PASS`（11桁）入力。**押すたびに毎回入力**（gmOKはセッション内のみ・保存しない）。全員のパスワード閲覧、写真チェック（〇×判定・取消）、お試しボード（写真アップ練習）、**「🎉 いまの結果発表をプレビュー」ボタン**（view=`gmresults`＝発表後と同じ画面＋GMプレビューnotice。名前タップ→個人ボード→戻るはプレビューに復帰＝`playerBack`変数）。写真チェック画面とお試しボードの下にはお題一覧（状態付き）と「これまでのきろく」（開閉式）が出る
4. 判定の伝わり方: マスに⭕/❌マーク＋履歴＋モーダル内notice。〇写真の削除・変更時は専用confirm。撮り直し・削除で判定はチェック待ちに戻る
5. みんなの状況: **発表前は特別ルール**＝`PROGRESS_VIEWER`（現在"あずみん"・設定ブロックの定数）本人だけは全員の進捗（バー＋n/60）が見え、他のメンバーにはPROGRESS_VIEWERの進捗だけが公開される（それ以外の情報は一切出さない）。加えて`PARITY_UNTIL`（8/9いっぱい）までは「🔮 自分の順位が奇数か偶数か」だけのヒントを表示（同率順位で計算・数字は非公開）。期限後は奇偶も非公開で「ヒント公開はおわったよ」表示。`RESULTS_AT` 後は結果発表＝①ランキング（**同率順位**・🥇🥈🥉と下位3人😈を点数の左に表示。😈は`DEVIL_EXEMPT`＝"あずみん"を除いた下から3人＋**その誰かと同順位の人にも付く**（同率対応。同じ順位なのに😈の有無が分かれないように。3人を超えることがある）。名前は`nameHtml`ヘルパーで男女色表示（男子=青#2E63B0・女子=赤#C23A3A。ランキング・集計ビンゴの回答者/まだの人・閲覧モーダル・個人ボードのヘッダーで使用）。計算式は「Xマス×1点＋ラインY本×2点＋FREE4マス×1点」表記）＋名前タップで写真入りボード（マスタップで`viewSquare`＝写真とコメントの拡大モーダル・閲覧専用・「📸 だれの写真か」を名前色付きで明示）②📊カテゴリー別まとめ表（`summaryTableHtml`＝左端固定=名前・6カテゴリー各10マスの埋め数（**×判定は除外＝点数と同じ数え方**）・ライン数・点数。10/10のマスは金色`.full`。行は順位順。名前タップで個人ボードへ。横スクロール＋sticky名前列）③🧩集計ビンゴ（全員の回答を重ねた盤面。人数バッジ・0人マスは薄表示。マスタップで`aggSquare`＝回答者の写真グリッド＋未回答者一覧、写真タップでviewSquareを重ね表示）。`?reveal=1` で発表前プレビュー（幹事用）。**発表後はトップ（名前選択画面）に「🎉 結果をみる」ボタン**が出て、**ログイン（名前選択・パスワード）なしで**結果発表画面が見られる（render()の `view==="results" && !me` 分岐→renderRevealed(false)。ログインなし時のみ「← 名前選択にもどる」`#resback`）。**発表後もマイビンゴの編集は従来どおり可能**（締切ロックは存在しない）。編集で順位が動くたびに結果画面は再描画されるため、renderRevealedは `.tblscroll`/`.boardscroll` の横スクロール位置を保存→復元している
6. localStorage: `pbingo_demo`（お試しモードのデータ）のみ。認証系キー（pbingo_name / pbingo_pass / pbingo_gm）は**廃止済み**（毎回パスワード入力方式のため）
7. ブラウザ/スマホの戻る・進むで1つ前の画面に戻れる（`render()` が画面=view＋ログイン名をhistoryに記録し、popstateで復元。同じ画面の再描画では積まない。別人のボードへは戻ってもパスワード照合で弾かれる）

## ローカル動作確認（必ずこの方法で）

本番データに触らない「お試しモード」で確認する:

1. `index.html` を一時フォルダにコピー
2. コピー内の `photo-bingo-4d3c1` を `ここに貼り付け` に置換（projectIdがダミーになるとlocalStorage完結のお試しモードで動く）
3. `npx http-server <一時フォルダ> -p 8090 -c-1` で配信してブラウザで確認（**file:// は不可**）
4. テストデータ投入: localStorageキー `pbingo_demo` に `{"boards":{...},"photos":{...}}` 形式
5. **リポジトリ本体の `FIREBASE_CONFIG` は書き換えたままcommitしないこと**

## 実装上の注意（ハマりどころ）

- `render()` は `app.innerHTML` 全置換方式。onSnapshotで誰かの更新のたびに再描画される。以下の配慮が入っているので、新しい画面を足すときも同様にすること:
  - パスワード入力中（`#pin`/`#gmpin` が存在）は再描画スキップ（入力が消えるため）
  - `.boardscroll` の横スクロール位置と、お題一覧 `<details>` の開閉状態を退避→復元
  - `renderGen` 世代トークン: async写真読み込みループは await 後に `gen!==renderGen` で中断（古いループが新DOMを上書きするのを防ぐ）
- `getPhotoCached` は `boards.photoTs` と照合するキャッシュ。写真を書き換える処理は必ず `setFilled` 経由で `photoTs` を更新すること
- 判定まわりは「**ボタンを押した時点の boards を再評価**」する設計（`openSquare` の `curJudge` / `gmSquare` の `stillSame`）。モーダルを開いている間に相手（GM⇔本人）が操作するレースが実際に起きるため、開いた時点の値で分岐しないこと
- モーダルのボタン配線は **写真の await より前** に行う（通信失敗でモーダルが閉じられなくなるのを防ぐ）
- `compress`: 長辺1400px・品質0.82→0.45のラダー、90万文字以下を目標。`URL.revokeObjectURL` を忘れない
- XSS: ユーザー由来の文字列（pass等）を innerHTML に入れるときは `esc()` を通す
- **`confirm()`/`alert()` は使用禁止**。LINE・Instagram等のアプリ内ブラウザや「ダイアログを今後表示しない」を押した環境では表示されず、confirmは常にキャンセル扱い→「ボタンを押しても反応しない」という報告になる（2026-08-04にGMの×判定で実際に発生）。確認・エラーはモーダル内の `#confirmarea` / `#merr`（GM側は `#gconfirmarea` / `#gmerr`）方式で出すこと
- パスワードの**桁数チェックは新規設定時だけ**掛ける（現在は5桁）。入場時は保存済みpassとの**一致のみ**照合する。桁数を4→5に変えた経緯があり、入場時に桁数で弾くと昔の桁数で設定した人が二度と入れなくなる
- 盤面はモバイル基準で縮小してあり、スマホ縦持ちで横スクロールなしの1画面に収まる。**640px以上で従来サイズに戻すmedia queryがある**ので、盤面まわり（.bc/.bgrid/.card/マーク類）のサイズを変えるときは基準側とmedia query側の両方を直すこと。お題ラベルの改行は `\n` の位置が正（文字サイズで収める。改行位置を勝手に変えない）
- `store.listenBoards(...)`（モジュール末尾）より下に `const`/`let` の定義を書かないこと。お試しモードのlistenBoardsは購読と同時に**同期で** `render()` を呼ぶため、未初期化変数の参照（TDZ）でモジュール初期化ごと落ちる。画面は表示されるのにタブ・名前選択が無反応になる形で現れ、本番のonSnapshot（非同期）では発症しないため気付きにくい（2026-08-04に実際に発生・修正）

## ボタンの役割ルール（UI/UX）

- **進む・開く系**（もっと見る／×判定一覧をみる／GMプレビュー等の誘導）: `.btn.more`＝薄紫塗り#EEEDFE＋紫太枠＋下影（押せる感を強調）
- **戻る系**（← 〜にもどる）: `.btn.back`＝グレー枠#C8C5BA・グレー文字・影なし（控えめ）。進むと戻るが並ぶ画面では必ず見た目で区別する
- **保存・実行**（モーダル内）: `.btn.primary`（紫塗り白字）、破壊系は`.btn.danger`、モーダル内の代替は`.btn.ghost`のまま
- 意味の違う塊の間（コンテンツ→ナビ等）には `.divider`（両端フェードの薄い線）を挟む
- タイムラインは日付が変わる境目に `.tldate`（「── 8/5(火) ──」）を挟む（`rejTimelineHtml`）

## 依頼主（ユーザー）の好み

- 平易な言葉＋コピペで済む手順。専門用語は噛み砕いて説明する
- 破壊的操作（force push・データ削除・公開設定変更）は必ず事前確認を取る
- 修正フロー: お試しモードで動作確認 → commit → push → 反映を確認して報告。生成・変更したファイルはパスを明示する

## 更新履歴（要約）

- 2026-08-04: 初期版に大型改修。①4桁パスワード制 ②GM画面（パスワード一覧・写真チェック）③〇×判定システム（履歴・点数除外・レース対策）④アップロード締切廃止 ⑤名前ボタン男女色分け ⑥ルール説明＋お題一覧表示 ⑦点数ボックス ⑧画質改善（長辺900→1400px）⑨GMお試しボード ⑩結果発表日時の非表示化
- 2026-08-04 (2): 名前選択を左列=男子/右列=女子の2列配置に変更（「男子/女子」の文字表記は削除、色分けは維持）。ブラウザの戻る/進むで画面遷移できるようhistory対応
- 2026-08-04 (3): 「ドパガキ卒業フォトビンゴ」に改名（ドパガキ治療プログラムの文言。デザインはポップさ維持）。お題一覧に自分のアップ状況（確認中/OK/要修正・カテゴリ別n/mコ）を表示。スマホ縦持ちで盤面が横スクロールなしの1画面に収まるよう縮小（640px以上は従来サイズ）。「いちらん」→「一覧」
- 2026-08-04 (4): 認証を毎回入力方式に変更（localStorage記憶を廃止、GMログアウトボタン削除）。GMパスワード8桁化（25802580）・個人パスワード5桁化（既存passは桁数不問で互換）。GMの写真チェック画面とお試しボードにも状態付きお題一覧を追加。あそびかたを簡潔化し、発表日時（RESULTS_AT=8/12 12:00）をあそびかた・countdownに表示
- 2026-08-04 (5): confirm/alertを全廃しモーダル内の確認・エラー表示に置換（アプリ内ブラウザでGMの×判定・写真削除が反応しない不具合の修正）
- 2026-08-04 (6): あそびかたを最小限の5項目に簡略化。治療プログラム導入文の末尾に🌈。あそびかた直下に「🏆1位には賞金！／😈卒業できなかった人には罰ゲーム！」の`.prize`ブロックを追加
- 2026-08-04 (7): 完成したラインが金色に光る演出を追加（litCells＋.lit、全ボード画面共通・データ層は不変更）。この時点でメンバーの実データ蓄積が始まったため、データ保護ルールに「実データ蓄積中」を明記
- 2026-08-04 (8): 状態色を全画面で統一（〇=緑#2F9E63・×=赤#C22B2B・確認中/未チェック/チェック待ち=黄#9A6B00系）。タイトルを「👦 ドパガキ卒業フォトビンゴ 👧」に
- 2026-08-04 (9): 写真コメント機能を追加（boards.comments・setComment。本人モーダルで記入/編集、GM判定モーダルと発表後の閲覧モーダル（viewSquare新設）で表示。写真削除時に一緒に削除。既存データ層は不変更）
- 2026-08-04 (10): 発表前の「みんなの状況」から進捗一覧を撤去し、発表予告メッセージのみに（発表までみんなの進み具合は完全非公開。発表後・?reveal=1は従来どおり）
- 2026-08-05: お題一覧を番号付き2列（各カテゴリ左1〜5・右6〜10）に変更。GMコメント機能を追加（boards.gmComments・判定モーダルで記入→本人のマスに🔑表示、閲覧モーダルにも表示。撮り直し/削除で自動削除）
- 2026-08-05 (2): 発表前の進捗に特別ルールを追加（PROGRESS_VIEWER="あずみん"だけ全員の進捗が見え、他メンバーにはあずみんの進捗だけ公開）
- 2026-08-05 (3): 結果発表を強化。同率順位（1,1,3方式）・🥇🥈🥉と下位3人😈（DEVIL_EXEMPT="あずみん"は免除）を点数の左に表示・計算式を「Xマス×1点＋ラインY本×2点＋FREE4マス×1点」表記に。🧩集計ビンゴ（全員合算盤面・人数バッジ・aggSquareで回答者写真グリッド＋未回答者一覧）を追加
- 2026-08-05 (4): ヘッダーをグラデーションバナー＋💊バッジに刷新（派手め・治療プログラム感）。進捗を「X/60マス・Y点」表記に。賞金文言を延長。GMパスワード11桁化（09042757073）。コメント機能の案内表示を追加。履歴を拡張（up/reup/cmt/gmcmt・コメント内容もlogに記録、見出し「これまでのきろく」）
- 2026-08-05 (5): ヘッダーを紫系に抑えめ調整し、下の要素に差し色（h2縦線・点数数字・進捗バーのグラデ）で調和をとる。ステージ制を追加（10点=2、15点=3、20点=4…100点=最終。バッジ＋あとX点バー＋到達演出）。賞金文言を「1位にはお祝いの賞金をプレゼント！」に短縮
- 2026-08-05 (6): 「これまでのきろく」を開閉式（histHtml・全件・内部スクロール）に変更し、GMの写真チェック画面にも表示。GM画面に「いまの結果発表をプレビュー」（gmresults）を追加。個人ボード閲覧に権限ガード（発表後 or gmOK）を追加
- 2026-08-05 (7): FREEマスを金リング＋深い影の「本当に穴が開いた」デザインに強化。GM画面に全員分の統合きろく（gmAllHist・名前付き）を追加。履歴の日時を曜日付きに、〇判定=緑・×判定=赤の背景色で視覚化
- 2026-08-05 (8): 順位の奇数/偶数ヒントを追加（PARITY_UNTIL=8/10 00:00まで表示・数字は非公開・期限後は奇偶も完全非公開）
- 2026-08-05 (9): きろくの視認性向上（コメント文字: 本人=緑/GM=オレンジ、行背景: コメント更新=薄青紫/GMコメント=薄オレンジ、日付が変わる境目に区切り線）
- 2026-08-05 (10): お題「変顔」を「変顔（自分限定）」に表記変更（r5c4・ラベルのみでマスIDは不変）
- 2026-08-05 (11): 結果発表まわりに名前の男女色を適用（`nameHtml`・男子=青/女子=赤。ランキング・集計ビンゴ・閲覧モーダル・個人ボード）。😈を同率対応（下から3人と同順位の人にも付与）。あそびかたに部分色付け（.hlp紫/.hlkピンク/.hlg緑/.hloオレンジ/.hlr赤）。閲覧モーダルに「📸 名前 の写真」を明示
- 2026-08-05 (12): 発表後の閲覧を強化。①トップ（名前選択画面）に発表後だけ「🎉 結果をみる」ボタン（.resultsbtn・金→ピンク→紫グラデ＋光る演出）②ログインなしで結果発表を閲覧可（view="results" && !me の分岐＋「← 名前選択にもどる」）③結果発表に📊カテゴリー別まとめ表を新設（summaryTableHtml。人×6カテゴリーの埋め数・ライン数・点数、×は除外、10/10金色、sticky名前列＋横スクロール、名前タップで個人ボード）④renderRevealedに横スクロール位置の保存/復元。発表後の編集可はもともとの仕様のまま（ロックなし）
- 2026-08-05 (13): ❌却下された回答集を追加（発表前から全員公開）。GMの×判定の瞬間に写真＋GMコメントを自動アーカイブ（photos/reject_*＋boards/_rejects。撮り直し・削除でも残る・同一写真の再×は重複スキップ）。GM画面に昔のスクショの手動登録モーダル（rejUploadModal・「GM登録」印）。ルール変更・データ移行なしの設計
- 2026-08-05 (14): 却下集を強化。手動登録の選択肢を「×判定を下した記録」だけに限定（人×お題のセット・×日時付き）、選ぶと当時の本人/GMコメントをログから自動復元。却下集の表示に💬本人コメント（緑）と🔑GMコメント（オレンジ）を併記、日時は「〜に×」（ngTs優先）。自動アーカイブにも本人コメント（cmt）を保存
- 2026-08-05 (15): ×アーカイブを再設計。GMのスクショ登録と「みんなの状況」内の却下集を撤去し、①トップ下部の「みんなの×判定一覧」ページ（盤面形式・×回数バッジ・お題タップで時系列の写真＋名前＋日時・写真タップでコメント付き拡大）②昔の×写真は本人がマイビンゴの「過去の×になった回答」から再アップ（未アップは「📤 アップロードしてください」表示・当時のコメント自動復元・保存で一覧に即反映）の2本立てに
- 2026-08-05 (16): 今も×判定中で写真が残っている昔の×は自動でアーカイブに取り込むように（rejAutoFillable＋backfillRejects。マイビンゴ表示時=本人分・×判定一覧表示時=全員分）。本人の再アップが必要なのは「撮り直しで写真が消えた×」のみに
- 2026-08-05 (17): ×判定一覧に🕒タイムライン追加（最新5件＋「もっと見る」で全件フルページ・Xふうカード仕切り・相対時刻fmtRel）。コメント表示の頭に発言者名（💬名前：／🔑ゲームマスター：）を全画面で統一。アーカイブ写真枠をdata-rejph方式に（複数箇所表示対応）
- 2026-08-05 (18): 再アップは「写真を選ぶだけ」に（当時のコメントは履歴から自動反映・履歴に無ければコメントなしで写真のみ）。タイムラインの時刻に正確な日付時刻を小さく併記（.tlexact・7日以上前は相対表記自体が日付なので省略）
- 2026-08-05 (19): UI/UX改善。タイムラインに日付区切り（.tldate）。ボタンを役割別に（進む=.btn.more薄紫塗り／戻る=.btn.backグレー控えめ）で全画面統一、意味の違う塊の間に.divider（薄い区切り線）を追加（トップ・×一覧・タイムライン・結果画面・GM画面）
- 2026-08-05 (20): GMの写真チェック画面に「×アーカイブの管理」（誤タップ重複の個別削除・2度押し確定式・removeReject）。「名前変更」ボタンを廃止し、ヘッダータップでどこからでもホームに戻る方式に（🏠ヒント付き）
